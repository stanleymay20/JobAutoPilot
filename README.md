# JobAutoPilot — Historical Predecessor

> **Status: superseded.** This repository is retained only to preserve project lineage.

The maintained job-application automation project is:

**https://github.com/stanleymay20/apply-wingman**

The current `JobAutoPilot` default branch is effectively empty. Its reachable pre-empty history contains repository-cleanup artifacts rather than recoverable application source, so new development should not be added here.

Apply Wingman contains the active React/Supabase application, job-discovery and application-tracking flows, Supabase Edge Functions, and Playwright ATS worker.

This repository remains public for historical reference rather than being deleted, so the project lineage is not erased.
